name: Migrations Check

on:
  pull_request:
    branches: [main]
    paths:
      - 'libs/backend/**/migrations/**'
      - 'libs/backend/**/infrastructure/src/lib/persistence/**'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: TODO — миграции будут проверяться здесь
        run: echo 'Migrations linting not yet implemented.'
